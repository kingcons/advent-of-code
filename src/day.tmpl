(mgl-pax:define-package :aoc.~4d.~2d
  (:nicknames :~4d.~2d)
  (:use :cl :aoc.util :mgl-pax))

(in-package :~4d.~2d)

(defsummary (:title "")
  "**Part 1** - "

  "**Part 2** - ")

(defun part-1 ()
  (let ((data (read-day-input #'foo)))
    (bar)))

(defun part-2 ()
  (let ((data (read-day-input #'baz)))
    (quux)))
