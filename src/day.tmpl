(mgl-pax:define-package :aoc.~4d.~2d
  (:nicknames :~4d.~2d)
  (:use :cl :mgl-pax :aoc.util))

(in-package :~4d.~2d)

(defsummary (:title "")
  "**Part 1** - "

  "**Part 2** - ")

(defun build-data (&optional input)
  (read-day-input #'identity :input input))

(defun part-1 (&optional input)
  (foo (build-data input)))

(defun part-2 (&optional input)
  (bar (build-data input)))
